# macOS
.DS_Store

# 大型音檔/影片 — 放 iCloud 備份不入 repo
*.m4a
*.webm
*.mp4
*.mkv

# Python
__pycache__/
*.py[cod]
*$py.class
.Python

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log
log/

# Claude session memory (user-specific, not for VCS)
memory/

# Local env files
.env
.env.*

# Build / packaging
build/
dist/
*.egg-info/

# Notebook checkpoints
.ipynb_checkpoints/

# FinMind cached raw data
data/raw/

# add_features() pickle cache (1.2GB+; regenerated on demand by backtest pipeline)
data/cache/

# Four-seasons classifier output (large CSVs ignored; .csv.zip archives committed)
data/analysis/four_seasons/season_*.csv
data/analysis/four_seasons/backtest_*_trades.csv
!data/analysis/four_seasons/backtest_2025_final_trades.csv
!data/analysis/four_seasons/*.csv.zip

# kline_patterns generated DBs (regeneratable backtest artifacts)
data/analysis/kline_patterns/*.db
data/analysis/kline_patterns/*.sqlite

# Runtime DBs at data/ root (regeneratable from FinMind / scanners)
data/*.db

# Backtest / scanner CSV outputs (regenerated each run)
data/analysis/kline/*.csv
data/analysis/zhuli/*.csv

# Sibling git worktrees (not source)
.claude/worktrees/
